@page "/"
<AuthorizeView>
    <Authorized>
        <p>Welcome, @context.User.Identity.Name!</p>
    </Authorized>
    <NotAuthorized>
        <p>You are not logged in. @context.User.Identity.Name</p>
    </NotAuthorized>
</AuthorizeView>