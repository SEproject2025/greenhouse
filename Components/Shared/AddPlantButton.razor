@inject NavigationManager NavigationManager
@rendermode InteractiveServer

<button @onclick="CreatePlant" class="btn btn-success">Add Plant</button>

@code {
    void CreatePlant()
    {
        NavigationManager.NavigateTo("/add-plants");
    }
}
